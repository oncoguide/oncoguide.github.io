{
  "name": "oncoguide-scripts",
  "private": true,
  "description": "Node tooling for the donation document flows (audit, fill-test). Not part of the Hugo site.",
  "type": "module",
  "scripts": {
    "audit": "node scripts/audit-contract-template.mjs"
  },
  "devDependencies": {
    "pdf-lib": "^1.17.1"
  }
}
